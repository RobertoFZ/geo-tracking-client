import styled from 'styled-components';

export type MarkerImageProps = {
}

const MarkerImage = styled.img`
  position: relative;
  margin: auto;
  top: -30px;
  left: 0;
`;

export default MarkerImage;